{
  "private": true,
  "scripts": {
    "start": "npm --prefix client start",
    "dist:win": "npm --prefix client run dist:win",
    "dist:linux": "npm --prefix client run dist:linux",
    "check:supabase": "npm --prefix server run check:supabase"
  }
}
